# 008 baseline — CI step list before any 008 code

**Date**: 2026-09-09 · **Branch**: `claude/spec-kit-integration-juhrza` ·
**Commit**: 687e56d (the last spec/plan/tasks commit, no 008 implementation yet)

**Task**: T003. Run the **real** CI step list from `.github/workflows/ci.yml`, in order, and
record the result — not a proxy for it. Two red builds in this project came from checking
typecheck, lint and tests and assuming that covered CI.

## Local table state (T002)

Dropped and reseeded before starting, per T002:

| | Items | Bytes |
|---|---|---|
| Before drop | 291 | 98,389 |
| After recreate + `seed:catalogue` | 12 top-level interests, catalogue only | — |

291 is small — nothing like the 3,875-people table that produced two false "regressions" in
007 — but the drop was done anyway so that Phase A's paging work starts from a known size.
**Check the table size before believing a bounded-page failure** remains the rule.

## Results

| Step | Result |
|---|---|
| `pnpm typecheck` | ✅ all five workspaces |
| `pnpm lint` | ✅ clean |
| `pnpm --filter @sih/infra db:create-local` | ✅ created `sih-main` with gsi1–gsi5 |
| `pnpm --filter @sih/infra s3:create-local` | ✅ bucket `sih-media` |
| `pnpm --filter @sih/infra seed:catalogue` | ✅ 12 top-level interests (idempotent) |
| `pnpm --filter @sih/api test` | ✅ **50 suites, 830 tests**, 36.4s, 4 projects |
| `pnpm --filter @sih/mobile test` | ✅ **28 suites, 190 tests**, 5.1s |
| `pnpm --filter @sih/workers test` | ✅ **1 suite, 6 tests** |

Remaining steps in the CI list (`generate:client`, `@sih/e2e test`, `smoke:boot`,
`test:durability`, `verify:stack`, `synth`, `verify:register`) are run at the Phase A gate
(T054–T059) rather than here; this baseline exists to establish that the tree was green
before 008 touched it, so any red during Phase A is attributable.

## Baseline numbers to compare against

- API **830** tests · mobile **190** tests · workers **6** tests.
- A drop in any of these during Phase A is a deletion, not a refactor, and must be explained.

## Environment note

`LOCAL_JWT_SECRET` has no default and was generated per-run. Docker was already up from an
earlier session; the two sandbox setup steps (registry mirror, `setsid nohup dockerd`) do
**not** survive a container reset and would need repeating after one.
